using UnityEngine;
using System.Collections;

public class S_Turret : S_Character
{
    //private Transform m_tPlayer;
	// Use this for initialization
	protected override void Start ()
    {
        /*m_tPlayer = GameObject.FindGameObjectWithTag("Player").transform;
        GetComponent<UnityStandardAssets.Utility.WaypointProgressTracker>().Ccircuit 
            = m_tPlayer.GetChild(8).GetComponent<UnityStandardAssets.Utility.WaypointCircuit>();*/
        //base.Start();
    }

    // Update is called once per frame
    protected override void Update ()
    {
        //base.Update();
	}
}
